{
  "name": "dcs-fiddle-httpd",
  "version": "1.0.0",
  "description": "DCS http server for use with DCS Fiddle",
  "license": "MIT",
  "scripts": {
    "install:windows": "choco install lua",
    "install:osx": "brew install lua",
    "clean": "rimraf dist",
    "dev": "cd src/ && nodemon -x lua init.lua",
    "build": "node bundle.js"
  },
  "devDependencies": {
    "luabundle": "^1.6.0",
    "nodemon": "^2.0.20",
    "platform": "^1.3.6",
    "prettier": "^2.8.3",
    "rimraf": "^4.1.2"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "zod": "^3.20.2"
  }
}
